[pytest]
asyncio_mode = auto
markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (require ES + Redis)
    e2e: End-to-end tests (full stack)
    edge: Edge case tests
testpaths = tests
